# Changelog — v0.2.0

## Corrected

- Moved product placement from a standalone AudioLab app to MUSIC → AudioLab → Glyph.
- Removed the proposed `apps/audiolab-glyph/` production path.
- Removed first-phase `packages/` architecture.
- Made MUSIC integration the MVP foundation rather than an exclusion.
- Added Looper, TrackInspector, Orb, and Geographic precedents.
- Added shared `glyphAnalyses` decision.
- Changed compositions toward `analysisId` references plus immutable snapshots/versioning.
- Added explicit Save / Save as New behavior.
- Added deterministic cache-key requirement.
- Added consolidated implementation plan and approved decisions.
- Updated repository safety and modification boundaries.
