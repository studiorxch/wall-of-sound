```
The exact pairing model
```

The 8 groups are:

```
The exact pairing model
```

Each mood also has:

```
The exact pairing model
```

So this chart is not just mood language. It is also already a **visual/color mood map**.

___

## The exact pairing model

Use this order:

```
The exact pairing model
```

Mechanical moods should **not replace** the 192 moods.

They should decide where in the chart a track is allowed to land.

___

## Example

A track analyzes as:

```
The exact pairing model
```

That should route the track toward:

```
The exact pairing model
```

Then intensity determines the exact region:

```
The exact pairing model
```

Then the chart gives candidate moods:

```
The exact pairing model
```

Now the 192 moods are no longer floating. They are constrained by structure.

___

## Mechanical mood → chart group mapping

# The exact pairing model

# The exact pairing model

| Mechanical mood |             Primary chart groups             | Common intensity  |
|-----------------|----------------------------------------------|-------------------|
|     `opener`      |         Trust, Joy, Wonder, Neutral          | Lower / Moderate  |
|     `closer`      |             Calm, Trust, Wonder              | Lower / Moderate  |
|     `bridge`      |        Neutral, Wonder, Dream, Trust         | Lower / Moderate  |
|      `reset`      |             Calm, Trust, Neutral             |       Lower       |
|      `lift`       |          Joy, Drive, Dream, Wonder           | Moderate / Higher |
|      `drop`       |              Drive, Fear, Calm               | Moderate / Higher |
|      `hold`       |             Neutral, Trust, Calm             | Lower / Moderate  |
|      `drift`      |             Calm, Wonder, Dream              | Lower / Moderate  |
|      `pulse`      |             Drive, Joy, Neutral              | Lower / Moderate  |
|     `tension`     |              Fear, Drive, Calm               | Lower / Moderate  |
|     `release`     |           Trust, Joy, Calm, Wonder           | Lower / Moderate  |
|      `build`      |          Drive, Dream, Wonder, Joy           | Moderate / Higher |
|     `plateau`     |            Neutral, Drive, Trust             |     Moderate      |
|    `recovery`     |               Calm, Trust, Joy               |       Lower       |
|   `transition`    |            Neutral, Wonder, Dream            | Lower / Moderate  |
|     `anchor`      | Any group, based on strongest catalog signal |        Any        |
|    `disruptor`    |         Drive, Fear, Dream, Neutral          | Moderate / Higher |
|    `deepener`     |          Calm, Fear, Neutral, Dream          | Moderate / Higher |
|   `brightener`    |              Joy, Wonder, Trust              | Lower / Moderate  |
|     `shadow`      |             Fear, Calm, Neutral              | Lower / Moderate  |

That table becomes the bridge.

___

## How to score it

For each track, PLAY should calculate:

```
The exact pairing model
```

A simple first-pass scoring model:

```
The exact pairing model
```

___

## Intensity pairing

The chart already has `Lower`, `Moderate`, `Higher`.

Use this kind of rule:

```
The exact pairing model
```

Then modify with brightness and density:

```
The exact pairing model
```

So intensity is not only “loudness” or “energy.” It is the track’s pressure level.

___

## How this helps the 198 moods

The old problem:

```
The exact pairing model
```

The new structure:

```
The exact pairing model
```

So a track does not choose from 192 moods blindly.

It chooses from maybe:

```
The exact pairing model
```

Then existing catalog mood fields can narrow further.

That is much more workable.

___

## Catalog fields to add

For each track:

```
The exact pairing modelThe exact pairing modelTypeScript
```

The chart fields should preserve the original `8x24_mood_chart.csv` IDs too:

```
The exact pairing modelThe exact pairing modelTypeScript
```

That lets a track connect directly back to the color/mood chart.

___

## Example track output

```
The exact pairing modelThe exact pairing modelJSON
```

___

## Key rule

```
The exact pairing model
```

That is the clean pairing.

The catalog should first analyze:

```
The exact pairing model
```

Then the mood chart answers:

```
The exact pairing model
```